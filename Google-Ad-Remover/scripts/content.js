const topAds = document.getElementById('tvcap');
const bottomAds = document.getElementById('bottomads');

//remove ads from page
topAds.parentNode.removeChild(topAds);
bottomAds.parentNode.removeChild(bottomAds);